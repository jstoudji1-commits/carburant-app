name: Mise a jour stations carburant

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - update_data.py
      - update_stations.py
      - .github/workflows/update-stations.yml

permissions:
  contents: write

concurrency:
  group: update-stations
  cancel-in-progress: false

jobs:
  update-stations:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Recuperer le projet
        uses: actions/checkout@v6

      - name: Installer Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.12"

      - name: Mettre a jour les donnees officielles
        run: python update_stations.py

      - name: Verifier si les donnees ont change
        id: changes
        run: |
          if [ -n "$(git status --porcelain -- stations.csv stations_metadata.json stations_prix_9h.json)" ]; then
            echo "changed=true" >> "$GITHUB_OUTPUT"
          else
            echo "changed=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Publier les nouvelles donnees
        if: steps.changes.outputs.changed == 'true'
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add stations.csv stations_metadata.json stations_prix_9h.json
          git commit -m "Mise a jour automatique des prix carburants"
          git push
